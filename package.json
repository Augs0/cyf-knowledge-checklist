{
  "name": "cyf-checklist",
  "version": "1.0.0",
  "description": "a checklist for students to check their understanding of modules on the course",
  "main": "scripts.js",
  "scripts": {
    "start": "rm -rf dist && rm -rf .cache && parcel index.html --no-cache --no-source-maps",
    "build": "rm -rf build && rm -rf .cache && parcel build index.html --no-cache --no-source-maps --out-dir build/",
    "reset": "rm -rf build && rm -rf .cache && rm -rf dist && rm -rf node_modules"
  },
  "author": "August Aldred",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "postcss-modules": "^3.2.2"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  }
}
